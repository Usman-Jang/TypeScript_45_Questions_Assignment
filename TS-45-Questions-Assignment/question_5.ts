/* Qs5. Famous Quote 2: Repeat Exercise 4, but this time store the famous person’s
name in a variable called famous_person.
Then compose your message and store it in a new variable called message.
Print your message. */

// Define the quote and its author 
let famousPerson: string = "IMRAN KHAN";
let quote: string = "Compromise for your Dream but NEVER Compromise on your Dream.";

// Compose the message
let message: string = `The Great ${famousPerson} once said, “${quote}”`;

// Print the message
console.log(message);