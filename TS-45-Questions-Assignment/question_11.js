"use strict";
/* QS.11 Names: Store the names of a few of your friends in a array called names. jPrint each person’s name by accessing each element in the list, one at a time. */
// Store the names of my friends in an array.
const friendsNames = ["Faraz", "Atique", "Ubaid", "Uraib"];
// Print each person's name by accessing each element in the list, one at a time.
friendsNames.forEach((value) => console.log(value));
